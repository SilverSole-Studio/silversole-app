// Deprecated: typography moved to `app_typography.dart` (exported by
// `theme.dart`). This misspelled file is kept only as a re-export shim so any
// stray import keeps compiling; it can be deleted.
export 'app_typography.dart';
